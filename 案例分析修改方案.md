# 案例分析部分修改方案（重点补齐部署后验证结果）

## 一、当前主要问题

1. 结构层级不稳定：各案例内部小节组织不一致，读者难以快速定位“复现结果、策略部署、部署后验证结果”。
2. 叙述链条不闭环：部分案例写了漏洞复现和策略建议，但“部署后是否有效”缺少可量化证据。
3. 证据表达分散：图、命令输出、文字分析没有形成统一证据链，影响说服力。
4. 三因素分析与实证脱节：有效性、无干扰性、可部署性分析有结论，但与“部署前后对比数据”绑定不够紧。

## 二、修改目标

将第六章每个案例统一为“问题—措施—验证—结论”的闭环，并以可复核证据回答三个问题：

1. 策略是否真正阻断了攻击路径（有效性）？
2. 策略是否引入明显业务干扰（无干扰性）？
3. 策略是否可稳定落地与复用（可部署性）？

## 三、推荐重构模板（每个案例统一）

每个案例建议固定为以下 6 个子段：

1. 漏洞背景与攻击链
2. 复现步骤与复现结果（部署前基线）
3. 策略部署（关键策略 + 部署命令）
4. 部署后验证结果（新增重点）
5. 三因素分析（严格引用前后对比证据）
6. 小结（一句话结论 + 适用边界）

说明：
- 第 4 段“部署后验证结果”必须是独立小节，不并入“策略部署”。
- 第 5 段不再只写概念性判断，必须引用第 4 段中的具体观测值。

## 四、“部署后验证结果”写作规范（核心）

### 4.1 每个案例至少给出三类证据

1. 攻击阻断证据：
- 部署前：攻击命令/路径可达（如 can-i 返回 yes、端口可达、接口 200）。
- 部署后：同一验证点失败（如 can-i 返回 no、端口拒绝、接口 401/403）。

2. 业务影响证据：
- 正常业务路径验证（如同步、拉取、控制面操作）仍可执行。
- 若有影响，明确影响范围与可接受条件。

3. 可部署性证据：
- 策略是否一次通过（语法/准入校验）。
- 是否依赖额外前置条件（版本、组件、权限）。

### 4.2 结果表达采用“前后对照表 + 关键截图”

每个案例建议新增一个最小对照表：

| 验证项 | 部署前 | 部署后 | 结论 |
|---|---|---|---|
| 攻击路径关键动作 | 成功/可达 | 失败/被拒绝 | 阻断成立 |
| 正常业务关键动作 | 正常 | 正常（或轻微影响） | 无干扰可接受 |
| 策略执行状态 | - | 已生效/可回滚 | 可部署 |

图证要求：
- 图 A：部署前关键证据
- 图 B：部署后同位置证据
- 图注直接写“部署前/部署后”，避免泛化措辞。

## 五、章节级调整建议（第六章）

1. 在“案例分析实验设计”中增加“部署后验证判据”小段，提前说明统一判据：
- 攻击阻断判据
- 业务可用判据
- 部署可行判据

2. 在 7 个案例后新增一张“部署后验证总表”（章末总表）：
- 每行一个案例
- 列包括：攻击阻断结果、业务影响、部署难点、最终判定

3. 现有“案例验证三维质量汇总表”改为“由部署前后证据驱动”的结论表，避免主观化表述。

## 六、分步执行计划

### 第一步：统一骨架（先结构后内容）

对 case1~case7 全部按 6 段模板统一标题与顺序，不改细节数据。

### 第二步：补证据（优先补第 4 段）

逐案例补“部署后验证结果”，至少补齐：
- 1 条攻击阻断证据
- 1 条业务可用证据
- 1 条部署生效证据

### 第三步：重写三因素分析

把“有效性/无干扰性/可部署性”全部改成“结论句 + 对应证据 + 边界说明”格式。

### 第四步：做章级汇总

新增“部署后验证总表”，并重写第六章小结，使其只基于已展示证据下结论。

## 七、可直接套用的段落模板（部署后验证结果）

模板 A（有效性）：

策略部署后，攻击链关键动作由“可执行”转为“被拒绝”。以 XX 验证点为例，部署前命令返回 XX（成功），部署后在相同条件下返回 XX（失败/拒绝），表明策略已在关键环节形成有效阻断。

模板 B（无干扰性）：

在阻断攻击路径的同时，策略对核心业务路径影响可控。针对 XX 正常操作进行复测，部署后仍可完成 XX，说明该策略未破坏必要业务能力；仅在 XX 场景下引入额外约束，需通过白名单或分阶段启用优化。

模板 C（可部署性）：

该策略由 Kubernetes 原生能力与成熟安全组件实现，部署链路清晰。策略文件通过 XX 校验并成功生效，回滚路径明确，具备在同类场景中复用的可行性。

## 八、优先级建议

优先级从高到低：

1. 先补“部署后验证结果”缺失最明显的案例。
2. 再统一所有案例的小节结构。
3. 最后优化文字风格与图表题注。

---

如果按此方案推进，建议先选 2 个案例做样板（例如 case1 与 case3），确认写法后再批量套用到其余 5 个案例，可显著减少返工。