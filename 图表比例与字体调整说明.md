# 图表比例与字体调整说明

本文档记录本次针对论文小版面展示做的图表比例与字体调整，方便后续继续手调。

## 1. 总体参数入口

- `images/analysis/plot.py:93-110`
  - 新增 `_COMPACT_JUDGE_SUMMARY_*`、`_COMPACT_HEATMAP_*`、`_COMPACT_RADAR_*` 常量。
  - 作用：集中管理小空间图表的画布尺寸、标题字号、坐标轴字号、图例字号、热力图注释字号。

- `images/analysis/plot.py:145-171`
  - `_style_axes()` 的坐标轴刻度字号从 `10` 调到 `11`。
  - `_style_colorbar()` 的色条刻度字号从 `9` 调到 `10`，色条标题字号从 `10` 调到 `11`。
  - 作用：提升论文缩放后的基础可读性。

- `images/analysis/plot_rank_order.py:27-67`
  - 为排序图补充中文字体优先级，并新增 `COMPACT_*` 常量。
  - 关键参数：
    - `COMPACT_RANK_FIGSIZE = (8.6, 4.7)`
    - `COMPACT_ICON_FIGSIZE = (9.0, 4.9)`
    - `COMPACT_TITLE_SIZE = 17`
    - `COMPACT_AXIS_LABEL_SIZE = 14`
    - `COMPACT_TICK_SIZE = 13`
    - `COMPACT_MARKER_SIZE = 210`
  - 作用：缩小画布、放大相对字号，适配论文中的小尺寸插图。

## 2. 方法/模型综合排序图

- `images/analysis/plot_rank_order.py:226-227`
  - `_y_map()` 从 `step=0.75, base=0.8` 改成 `step=0.68, base=0.72`。
  - 作用：压缩纵向留白，让 3 个维度在更紧凑的版面里更协调。

- `images/analysis/plot_rank_order.py:248-308`
  - 调整模型图例面板的位置、标题字号、图例字号、图标缩放。
  - 关键修改：
    - 图例面板位置：`[0.80, 0.16, 0.18, 0.68]`
    - 图例标题字号：`COMPACT_LEGEND_TITLE_SIZE`
    - 图例文字字号：`COMPACT_LEGEND_TEXT_SIZE`
    - 图例图标缩放：`COMPACT_ICON_LEGEND_ZOOM`
  - 影响图片：`images/output_rank_order/profile_rank_order.png`

- `images/analysis/plot_rank_order.py:322-399`
  - 方法排序图改用 `COMPACT_RANK_FIGSIZE`。
  - 标记点大小改为 `COMPACT_MARKER_SIZE`。
  - 标题、坐标轴标题、刻度字号统一通过 `COMPACT_TITLE_SIZE`、`COMPACT_AXIS_LABEL_SIZE`、`COMPACT_TICK_SIZE` 提升。
  - 图例 marker 大小改为 `COMPACT_MARKER_LEGEND_SIZE`，图例文字/标题字号单独提升。
  - 影响图片：`images/output_rank_order/method_rank_order.png`

- `images/analysis/plot_rank_order.py:403-446`
  - 模型排序图改用 `COMPACT_ICON_FIGSIZE`。
  - 图标缩放改为 `COMPACT_ICON_ZOOM`。
  - 标题、坐标轴标题、刻度字号统一按紧凑版参数放大。
  - 影响图片：`images/output_rank_order/profile_rank_order.png`

## 3. 单维度“方法 × 评委模型”柱状图

- `images/analysis/plot.py:651-706`
  - 画布从 `10.6 x 4.85` 改为 `_COMPACT_JUDGE_SUMMARY_FIGSIZE = (8.4, 4.9)`。
  - 通过 `fig._suptitle.set_fontsize(...)` 和 `set_y(...)` 提升并微调标题位置。
  - 坐标轴标题字号提升到 `_COMPACT_JUDGE_SUMMARY_LABEL_SIZE = 13`。
  - 横纵轴刻度字号提升到 `_COMPACT_JUDGE_SUMMARY_TICK_SIZE = 11`。
  - 图例重新锚定到 `0.81` 高度，并尽量压成最多 `3` 列。
  - 柱顶数值字号提升到 `_COMPACT_JUDGE_SUMMARY_BAR_LABEL_SIZE = 10`。
  - `tight_layout(rect=...)` 从 `0.80` 改到 `0.84`，给标题和图例更合理留白。
  - 影响图片：
    - `images/plots/可部署性/summary/single_dim_by_judge_可部署性.png`
    - `images/plots/无干扰性/summary/single_dim_by_judge_无干扰性.png`
    - `images/plots/有效性/summary/single_dim_by_judge_有效性.png`
    - 同步刷新到 `images/analysis/plots/...`

## 4. 评委相关性热力图

- `images/analysis/plot.py:1084-1111`
  - 画布从 `7.2 x 6.0` 改为 `_COMPACT_HEATMAP_FIGSIZE = (6.2, 5.5)`。
  - 热力图增加 `square=True`，让方格在小版面下更整齐。
  - 数字标注通过 `annot_kws={"size": _COMPACT_HEATMAP_ANNOT_SIZE, "weight": "medium"}` 放大。
  - 色条字号通过 `_style_colorbar()` 同步抬高。
  - 影响图片：
    - `images/plots/judge_consistency/judge_correlation_heatmap_有效性.png`
    - `images/plots/judge_consistency/judge_correlation_heatmap_无干扰性.png`
    - `images/plots/judge_consistency/judge_correlation_heatmap_可部署性.png`
    - 同步刷新到 `images/analysis/plots/judge_consistency/...`

## 5. 大语言模型评审雷达图

- `images/analysis/plot.py:711-780`
  - 整体雷达图改用 `_COMPACT_RADAR_FIGSIZE = (6.4, 6.4)`。
  - 维度标签字号改为 `_COMPACT_RADAR_DIM_LABEL_SIZE = 13`。
  - 圆环刻度字号改为 `_COMPACT_RADAR_RING_LABEL_SIZE = 10`。
  - 折线宽度从 `3` 调到 `2.8`，避免缩小后线条过粗。
  - 标题字号改为 `_COMPACT_RADAR_TITLE_SIZE = 15`，图例字号改为 `_COMPACT_RADAR_LEGEND_SIZE = 11`。
  - 图例锚点从 `(1.28, 1.10)` 调到 `(1.24, 1.08)`，让右侧留白更紧凑。
  - 影响图片：
    - `images/plots/summary/radar_generator_overall.png`
    - `images/plots/summary/radar_method_overall.png`
    - 同步刷新到 `images/analysis/plots/summary/...`

- `images/analysis/plot_gpt_judge_radar.py:32-36, 115-154`
  - 将 GPT-5 单评委雷达图同步到与 `plot.py` 相同的小版面参数。
  - 作用：后续单独重画 `radar_generator_gpt5.png` / `radar_method_gpt5.png` 时，风格不会漂移。

## 6. 人工评审雷达图

- `images/human/analyze_human_review.py:64-68`
  - 新增 `COMPACT_RADAR_*` 常量，和 LLM 雷达图保持一致。

- `images/human/analyze_human_review.py:287-334`
  - 人工评审整体雷达图改用 `6.4 x 6.4` 紧凑比例。
  - 维度标签、圆环刻度、标题、图例字号统一提升。
  - 图例锚点同步改到 `(1.24, 1.08)`，避免右侧空白过大。
  - 影响图片：
    - `images/human/analysis/human_generator_radar_overall.png`
    - `images/human/analysis/human_method_radar_overall.png`

## 7. 额外兼容性修复

- `images/analysis/plot_rank_order.py:32-42`
  - 补了中文字体优先级，避免重画时中文标签掉成方块或缺字。

- `images/analysis/plot_rank_order.py:76-80`
  - 新增 `_to_markdown_safe()`，在没有 `tabulate` 时退化成 `to_string()`。
  - 作用：保证重跑排序图脚本时，即使环境里没装 `tabulate`，图片和说明报告也能正常生成。

## 8. 本次已重画的文件

- `images/output_rank_order/method_rank_order.png`
- `images/output_rank_order/profile_rank_order.png`
- `images/plots/judge_consistency/judge_correlation_heatmap_有效性.png`
- `images/plots/judge_consistency/judge_correlation_heatmap_无干扰性.png`
- `images/plots/judge_consistency/judge_correlation_heatmap_可部署性.png`
- `images/plots/可部署性/summary/single_dim_by_judge_可部署性.png`
- `images/plots/无干扰性/summary/single_dim_by_judge_无干扰性.png`
- `images/plots/有效性/summary/single_dim_by_judge_有效性.png`
- `images/plots/summary/radar_generator_overall.png`
- `images/plots/summary/radar_method_overall.png`
- `images/analysis/plots/...` 对应同名文件
- `images/human/analysis/human_generator_radar_overall.png`
- `images/human/analysis/human_method_radar_overall.png`

## 9. 后续追加修改

- `images/analysis/plot.py`
  - 删除了“评审模型单维度评分对比”对应 `single_dim_by_judge_*.png` 的图内标题。
  - 同时把这组图的图例字号继续调大，并把 `tight_layout(rect=...)` 放宽到 `0.88`。

- `images/analysis/plot.py`
  - 删除了大语言模型评审整体雷达图的图内标题。
  - 同时把雷达图图例字号从原来的紧凑版继续调大到 `13`。

- `images/human/analyze_human_review.py`
  - 删除了人工评审整体雷达图的图内标题。
  - 同时把雷达图图例字号调大到 `13`。
