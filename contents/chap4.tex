\chapter{面向云原生权限提升智能化漏洞防控系统工具}\label{chap:system}

\section{需求分析}

云原生场景下的权限提升（Kernel/Container Privilege Escalation, KPE）漏洞证据分散、修复策略复杂，且往往需要在安全性与业务影响之间权衡。基于此，本系统原型工具按 human-in-the-loop（人在回路）范式设计：模型负责生成候选与给出建议，人类负责证据核验、策略修订与最终决策，并通过结构化留痕保证过程可追溯、结果可复现。整体流程以``证据汇聚---策略生成/优化---质量评价与审阅---发布与回滚''为闭环。

\subsection*{目标与范围}
本系统聚焦于K8S权限提升漏洞的监控策略生成与质量评估：从证据获取、生成与优化，到审阅决策与留痕回滚。原型阶段不直接对接真实集群执行，而将``发布''定义为输出可落地策略与采纳决策，并将证据、产物与决策过程持久化，支撑复现与后续工程化。

\subsection*{用户角色与使用场景}
系统面向以下典型角色：
\begin{itemize}
	\item \textbf{安全工程师/平台运维（SRE）}：浏览漏洞证据与候选策略，评估有效性与风险，给出采纳或带监控采纳的决策，并对策略做必要的人工修订。
	\item \textbf{评审/标注人员}：对漏洞或策略进行三维评分并给出评语，形成可审计的人工标注数据，用于后续分析或学习排序偏好。
	\item \textbf{研究/算法工程师}：对多方法/多模型输出进行对比，查看统计仪表盘与样例钻取结果，分析不同方法在三维指标上的表现差异。
\end{itemize}

\subsection*{功能性需求}
结合原型实现，本系统的核心功能需求如下：
\begin{enumerate}
	\item \textbf{证据与知识汇聚}：支持加载漏洞清单与 CVE 原文（Markdown），展示第三方应用与漏洞摘要等结构化字段，并支持多模型/多方法结果的聚合浏览。
	\item \textbf{交互式策略生成与优化}：提供端到端管线会话（session）管理，支持分阶段执行检索、规划、推理、策略生成、评审与优化；支持人工在环对中间产物进行编辑回写。
	\item \textbf{三维质量评价与审阅}：对候选策略进行``有效性''、``无干扰性''、``可部署性''三维评价，支持打分、评语与自动给出采纳建议；同时支持对同一 CVE 的多候选策略进行按维度排序，形成偏好数据。
	\item \textbf{可视化分析与对比}：基于离线汇总文件展示不同方法/评测器在三维维度上的总体表现，并提供样例钻取以支撑定性分析。
	\item \textbf{发布与回滚（原型定义）}：将发布视为``输出可落地策略并记录采纳决策''，回滚视为``撤销/更改历史决策并保留审计轨迹''。
\end{enumerate}

\subsection*{非功能性需求}
为满足论文实验与原型验证，系统还需满足：
\begin{itemize}
	\item \textbf{可用性}：界面清晰、流程可视，评审操作成本低；支持容器化一键启动。
	\item \textbf{可追溯与可审计}：所有评分、排序与决策均需落盘留痕，能够复现评审过程与结果。
	\item \textbf{可扩展性}：方法（method）、评测器（judge）、维度（dimension）等应尽量数据驱动，便于追加新模型或新实验输出。
	\item \textbf{轻量化}：原型阶段采用 CSV/JSON 作为主要存储介质，降低部署复杂度，并为后续迁移至数据库预留空间。
\end{itemize}

\section{系统设计}

\subsection*{总体架构}
系统采用``前端展示层---后端服务层---数据与证据层---智能体管线层''的分层架构，前后端通过 REST API 交互。图~\ref{fig:system-arch} 给出逻辑架构与数据流。

\begin{figure}[htbp]
	\centering
	\fbox{\parbox{0.92\linewidth}{
	\textbf{系统逻辑架构（示意）}\par
	前端（Vue/ElementPlus/ECharts）\;\;\(\leftrightarrow\)\;\; 后端 API（Flask）\;\;\(\leftrightarrow\)\;\; 数据/证据（CSV/JSON/Markdown）\par
	\quad\; \(\downarrow\) 交互式管线编排：检索\(\rightarrow\)规划\(\rightarrow\)推理\(\rightarrow\)策略\(\rightarrow\)评审\(\rightarrow\)优化\par
	\quad\; \(\downarrow\) 质量评估：三维评分 + 候选排序 + 统计仪表盘\par
	\quad\; \(\downarrow\) 发布/回滚：采纳决策与审计留痕
	}}
	\caption{面向云原生权限提升漏洞防控系统原型总体架构}
	\label{fig:system-arch}
\end{figure}

\subsection*{Human-in-the-loop 设计原则}
本原型工具将``人在回路''作为核心设计约束：系统不追求完全自动化替代人工，而是把专家判断放在关键节点，并以留痕机制确保可解释、可追溯。主要体现在：
\begin{itemize}
	\item \textbf{证据先行}：生成与评审都以可查看的证据为前提。
	\item \textbf{可编辑会话产物}：管线分阶段产物支持人工修改并回写，形成``生成---校正---再生成''的闭环。
	\item \textbf{人最终决策 + 全量留痕}：系统给出建议，人类确认决策；评分、排序、评语与决策可复盘。
\end{itemize}

\subsection*{模块划分与职责}
结合代码实现，系统可划分为五类核心模块（与后续实现小节对应）：
\begin{itemize}
	\item \textbf{核心模块（展示与编排）}：前端提供多视图入口与交互控件（表格、表单、图表、会话面板等），后端提供统一 API 网关与轻量存储。
	\item \textbf{证据与知识库服务}：负责读取漏洞证据（结构化 CSV、CVE Markdown）与多模型聚合结果，并为管线检索阶段提供缓存目录与索引。
	\item \textbf{策略生成与优化服务}：以会话为单位编排分阶段执行，产出包括检索证据、推理过程、结构化策略、评审意见与优化后的策略。
	\item \textbf{质量评价与审阅}：提供三维评分、候选排序、统计仪表盘与样例钻取；并将人工评审数据持久化为可追溯记录。
	\item \textbf{策略发布与回滚}：在原型阶段，将策略``发布''抽象为采纳决策与策略版本的落盘归档；``回滚''抽象为对历史决策的撤销/更改，并保留全量审计轨迹。
\end{itemize}

\subsection*{数据与接口设计}
原型系统遵循``数据驱动''原则：系统输入与输出均以可读的文件格式呈现，并由后端 API 统一封装。主要数据对象包括：
\begin{itemize}
	\item \textbf{漏洞证据数据}：包含 CVE ID、第三方应用、漏洞摘要、风险等级等字段的结构化表（CSV），以及 CVE 原文（Markdown）。
	\item \textbf{候选策略数据}：对每个 CVE 生成的策略候选集合（JSON），用于排序评审与对比分析。
	\item \textbf{人工评分记录}：针对 CVE 或策略的三维评分与评语（CSV），用于审计与统计。
	\item \textbf{排序历史记录}：针对某个 CVE、某种方法、某个维度的候选策略排序结果（CSV），用于偏好数据沉淀。
	\item \textbf{匿名化映射}：候选策略 ID 与其来源元信息的映射（JSON），用于盲审与追溯之间的折中。
\end{itemize}

接口层采用 REST 风格，按功能聚类为：证据读取接口、评分接口、排序接口、分析接口与交互式管线接口。该设计一方面保证前端页面的解耦与可扩展，另一方面便于在原型阶段快速迭代与复现实验。

\section{系统实现与演示}

系统原型工具位于 \texttt{ui/} 目录下，采用前后端分离实现：
\begin{itemize}
	\item \textbf{后端}：基于 Flask 实现 REST API，并对 \texttt{/api/*} 路径开放跨域访问；采用 CSV/JSON/Markdown 文件作为主要存储与证据载体。
	\item \textbf{前端}：基于 Vue3 + Vite 实现单页应用（SPA），使用 ElementPlus 构建交互控件，使用 ECharts 绘制统计图表（如热力图）。
	\item \textbf{部署方式}：支持 \texttt{docker-compose} 一键启动前后端服务，也支持本地脚本启动以便开发调试。
\end{itemize}

为便于说明，后续从核心模块开始，对证据与知识库、策略生成与优化、质量评价与审阅，以及发布与回滚的原型实现分别展开。

\subsection{核心模块}

核心模块负责把``证据浏览---生成/优化---评分/排序---决策留痕''组织为一套可操作的界面流程。前端以多视图入口承载不同任务（证据浏览、人工评分、候选排序、仪表盘、交互式管线），后端提供统一的 API 与轻量存储以支撑页面交互。

在存储上，原型采用``结构化字段 + 追加写入''方式记录评分、排序与决策，使交互过程可追溯、可复现，并便于后续迁移至数据库。

\subsection{证据与知识库服务}

证据与知识库服务提供两类输入：一是漏洞证据（结构化清单与 CVE 原文），二是多方法/多模型生成产物（用于对比分析与评测）。后端将其统一封装为可调用接口，前端以``可阅读证据''的形式呈现给评审者；交互式管线会把检索结果落入会话缓存，以支撑复现与审计。

\subsection{策略生成与优化服务}

策略生成与优化服务以会话为单位编排端到端流程，并将每次生成的中间产物纳入会话上下文以便追溯。原型将流程抽象为``检索---推理---生成---评审---优化''五类阶段；其中评审者可对任一阶段产物进行修改并回写，使后续阶段在``人工校正''后继续推进，体现 human-in-the-loop 的闭环特征。策略最终以结构化形式输出，便于阅读、评审与版本化管理。

\subsection{质量评价与审阅}

\subsubsection*{三维评价体系与打分机制}
为支撑策略质量的统一度量，系统原型采用三维评价体系：
\begin{itemize}
	\item \textbf{有效性（Effectiveness, $E$）}：策略对权限提升攻击路径的覆盖程度与防护有效性。
	\item \textbf{无干扰性（Non-interference, $N$）}：策略对正常业务的影响程度，越不干扰得分越高。
	\item \textbf{可部署性（Deployability, $D$）}：策略在云原生环境中的落地成本、可操作性与运维复杂度。
\end{itemize}

评分采用 $0\sim 10$ 的量表，并给出加权总分：
\[
S = 0.50\cdot E + 0.35\cdot N + 0.15\cdot D.
\]
总分映射为三档建议（采纳/带监控采纳/拒绝），并由评审者结合业务背景做最终确认；评语用于记录关键依据，形成可复盘的审阅结论（human-in-the-loop）。

\subsubsection*{候选策略排序与偏好数据沉淀}
系统支持对同一 CVE 的多候选策略进行按维度排序，并将排序历史持久化为偏好数据。为降低来源偏见，候选可匿名化展示；需要审计时可通过映射追溯来源。

\subsubsection*{统计仪表盘与样例钻取}
系统提供基于离线汇总文件的对比仪表盘，用于观察不同方法在三维维度上的整体表现，并支持样例钻取以辅助解释与复盘。

\subsection{策略发布与回滚}

\subsubsection*{原型阶段的发布定义}
原型阶段``发布''不直接执行到真实集群，而是将发布定义为：选定策略版本、给出采纳决策、并完成留痕归档。该定义强调 human-in-the-loop：发布是评审确认驱动的决策过程，而非单次自动动作。

\subsubsection*{回滚与版本化留痕}
``回滚''体现为对历史决策的撤销或更改：当证据变化、业务反馈提示干扰、或更优版本出现时，系统允许重新评审并提交新的记录。通过追加写入保留决策链路，从而支持追溯与复现。

\subsubsection*{演示流程（端到端）}
原型系统的端到端演示可概括为：证据浏览与核验；会话化生成与人工修订；评分/排序形成审阅结论；发布决策与留痕回滚。


