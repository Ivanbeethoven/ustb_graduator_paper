# 第三章最终目录结构与撰写思路

> 调整点：① 定义前置于框架图；② 定义4移至质量评价节；③ 质量评价升为独立二级标题。

---

## 最终目录

```
3.1 研究动机
3.2 方法框架
    3.2.1 基本概念定义
        基本对象（定义1/2/3 + 子定义3.1/3.2/3.3）
        候选策略集合说明（Πv定义）
    [框架图及整体流程概述]
    3.2.2 云原生权限提升安全知识库
        数据采集
        漏洞筛选
        漏洞分类
        知识库构建
    3.2.3 基于多智能体协同的策略生成与优化
        安全上下文构建
        基于漏洞分析思维链的策略生成
        基于评审反馈的策略优化
    3.2.4 基于排序的质量评价
        定义4（三维质量向量）
        共识排序与分歧度量
        总体质量函数与权重配置
3.3 方法示例（CVE-2024-33522）
3.4 小结
```

---

## 各节撰写思路

### 3.1 研究动机

**目标**：回答"为什么要做这件事"，驱动读者产生"这个问题值得研究"的判断。

**逻辑链**：
1. 引出补丁窗口期问题：漏洞披露后补丁无法立即部署，攻击者却可迅速还原利用条件 → 以统计数据（30.4% 长期可利用）支撑危害程度
2. 说明现有工具的不足：现有工具（扫描、准入控制、运行时监控）覆盖通用基线，但缺少"围绕具体漏洞证据组织临时策略"的能力
3. 大语言模型的切入点：LLM 能从公告中提取攻击语义并与已有工具衔接，但直接使用存在幻觉、不确定性等问题
4. 提出三个关键问题（证据组织 / 语义映射 / 结果不确定性），自然引出多智能体协同方案

**注意**：此节偏背景叙述，不要出现方法细节；三个关键问题应与后文 3.2.2/3.2.3/3.2.4 节的设计形成对应关系，便于读者在阅读方法时回溯动机。

---

### 3.2 方法框架

**节首段（引言）**：一段话概括整体思路：以漏洞实例为输入，通过知识库构建、多智能体协同和质量聚合三个子系统，输出可部署的临时策略。宣告本节的展开结构，即先给定义，再给框架图，再依次展开各子系统。

---

#### 3.2.1 基本概念定义

**目标**：统一全章符号，建立从漏洞到策略的形式化骨架。

**逻辑顺序**：
1. **定义1 漏洞实例 v**：引入三元组 ⟨id, d, m⟩，说明三个字段分别对应 CVE 编号、描述文本和元数据；强调这是知识库构建的输入单元。
2. **定义2 安全上下文 c**：说明证据集合带来源标识的必要性（可追溯、可复核）；强调 c 不等于 v，是从知识库中检索后重组的当前相关证据。
3. **定义3 安全策略 π 及子定义3.1/3.2/3.3**：先给出三元组 ⟨type, method, action⟩ 的整体结构，再逐一定义 type/method/action；在子定义中用具体例子（Falco 规则=mon，SUID修复=prot）帮助读者快速理解。
4. **候选策略集合 Πv**：说明对单个漏洞 v 可生成多个候选，按类型分为 Πv_mon 和 Πv_prot，为后续评价打基础。

**提示**：此处不展开评价指标（定义4），避免与质量评价节重复；可在文末加一句"策略质量评价所涉及的维度定义将在 3.2.4 节进一步给出"。

---

**[框架图位置]**（定义节之后，3.2.2 之前）

在给出定义后，用一段话引出框架图："基于上述定义，本文构建如图 X 所示的整体框架……"，图后再用一段话逐模块概括数据流（输入→知识库→多智能体生成→质量评价→输出）。这样读者带着已有定义读图，能准确理解每个模块的语义。

---

#### 3.2.2 云原生权限提升安全知识库

**目标**：说明如何把分散的多源证据组织成可追溯、可检索的知识库 Kv，为后续策略生成提供稳定的输入。

**撰写思路**：

- **节首段**：明确知识库构建的地位——它是整个方法能够"面向具体漏洞"而非泛化建议的关键支撑；简述四个子步骤的逻辑关系（采集→筛选→分类→向量化构建）。

- **数据采集**：说明来源（CNCF Landscape + NVD/MITRE/GHSA），强调多源融合动机（单一数据库有遗漏），一句话带过实现细节。

- **漏洞筛选**：重点说明三重过滤逻辑（关键词 + CVSS ≥5.0 + 人工复核），以及筛选后字段与定义1中 v=⟨id,d,m⟩ 的对应关系——让读者看到定义与数据实现的一一映射。

- **漏洞分类**：先说明为何需要分类（CWE 不够直接对应防控动作，且多标签导致歧义）→ 引出两级框架 → 给出分类表 → 说明分类结果如何用于提示词约束（策略生成阶段的前置语义线索）。四类成因的详细说明可适当压缩（每类2-3句），把重点放在"分类如何服务于后续生成"上。

- **知识库构建**：按流程说明分块策略（代码按行/函数对齐，文档按段+重叠窗口）→ 向量化（MPNet + FAISS）→ 持久化（索引+元数据+构建配置）；重点强调"来源标识"和"可复现性"——这是与定义2中 src_i 对应的工程实现。

---

#### 3.2.3 基于多智能体协同的策略生成与优化

**目标**：说明五类智能体如何将 (v, c) 转化为候选策略集合 Πv，并通过评审反馈迭代优化。

**撰写思路**：

- **节首段**：概括五智能体分工（知识整理→漏洞分析→策略生成→评审→优化），说明 RAG+CoT+反馈优化三类机制各自在哪个环节起作用；明确本节结束产出的是 Πv（不含最终质量排序，排序见 3.2.4）。

- **安全上下文构建**：说明安全知识智能体的职责是去重、提炼、保留来源；强调其输出是结构化摘要（不是策略），与定义2的 c 直接对应；以提示词简化示例辅助说明输入输出边界。

- **基于漏洞分析思维链的策略生成**：分两步说明——① 漏洞分析智能体：基于 c 形成结构化分析（类型/路径/范围/成因），与分类框架对齐；② 策略生成智能体：从分析结论出发选择控制手段（RBAC收敛/网络隔离/准入控制/运行时约束），映射为三元组策略对象，保留假设条件；给出提示词简化示例，并对照表格（多智能体指令约束摘要）点明约束来源。强调"先分析再生成"的思维链机制相比直接生成的优势（减少幻觉/保证字段完整）。

- **基于评审反馈的策略优化**：说明评审→优化的迭代逻辑：评审智能体按有效性/无干扰性/可部署性给出结构化意见 → 优化智能体合并可行措施、剔除不可行建议、补齐前置条件与回滚方案；本节重点不是模板表述，而是"评审信息如何被吸收"；给出提示词简化示例。

---

#### 3.2.4 基于排序的质量评价

**目标**：说明如何对 Πv 中的候选策略进行可审计的多维排序，得到最终交付结论。

**撰写思路**：

- **节首段**：解释为什么用相对排序而非绝对分值（跨漏洞尺度漂移问题）；说明先按类型划分（Πv^τ）再在同质子集内排序的动机（mon 与 prot 目标不同质，不应混排）；引出定义4。

- **定义4（三维质量向量）**：q(π) = (E, I, D)，分别说明有效性/无干扰性/可部署性的物理含义；强调这三维正好对应研究动机中"如何控制不确定性"的三个子问题。

- **共识排序与分歧度量**：说明多评审主体下如何用加权平均秩 r̄ 得到共识排序 R̄_δ；引入分歧度量 U_δ 用于标记争议候选（U_δ 大时人工复核）；简要说明如何打破平分。

- **总体质量函数与权重**：给出 Q(π) = α_E·Ŝ_E + α_I·Ŝ_I + α_D·Ŝ_D；说明权重的可配置性（应急场景提高 α_E/α_D，稳定生产提高 α_I）；给出默认基线（0.5/0.3/0.2）并简要说明敏感性分析（合并为一段，不要拆散）；最后说明最终排名 R̄^τ 的确定性保证。

---

### 3.3 方法示例

**目标**：通过 CVE-2024-33522 完整走通一次方法流程，让读者对定义与模块有直观感受。

**节首段**：明确这是演示性内容（不是实验验证），说明选取该漏洞的理由（节点侧权限提升，涉及多种控制手段，适合展示全流程）。

**步骤1-4 的撰写**：每步开头先点出在方法框架中的对应位置（如"按照定义2/步骤"），再给出具体数据和操作，最后总结该步的输出；步骤间有一句衔接。

**结尾**：用一段话将示例结果回归到方法层面——说明最终策略对象是经过定义、证据支撑、智能体协同和质量聚合得到的，不是泛化建议。

---

### 3.4 小结

**目标**：收束全章，回应 3.1 中的三个关键问题。

**建议结构**：
- 第1句：本章目标/任务是什么（一句话概括）
- 第2-4句：三个关键问题分别被哪个设计解决（知识库→证据组织；多智能体思维链→语义映射；评审+排序→不确定性控制）
- 最后一句：指向下一章（系统设计与实现 / 实验验证）


## 一、当前结构梳理

```
3.1 研究动机
3.2 方法框架
    3.2.1 基本概念定义
        基本对象（定义1/2/3/3.1/3.2/3.3）
        质量评价指标（定义4）
    3.2.2 云原生权限提升安全知识库
        数据采集
        漏洞筛选
        漏洞分类（框架介绍 + 四类成因详述）
        知识库构建
    3.2.3 基于多智能体协同的策略生成与优化
        安全上下文构建
        基于漏洞分析思维链的策略生成
        基于评审反馈的策略优化
        基于排序的质量评价
3.3 方法示例（CVE-2024-33522 完整走读）
3.4 小结
```

> 结论：在方法框架节内依次讲各子模块的写法完全合理，无需大动结构。

---

## 二、已确认的改动

### ✅ 定义前置于框架图（已确认执行）

**问题**：当前 3.2 节先给出总体框架图，再给出定义 1/2/3，读者看图时符号（π、c、Kv）尚未定义，需要向后翻页。

**解决方案**：把"基本概念定义"子节（3.2.1）整体移到框架图之前，即：
```
3.2 方法框架
    框架图概述段（不含公式符号，仅自然语言描述各模块）
    3.2.1 基本概念定义（定义1/2/3/3.1/3.2/3.3 + 定义4）
    3.2.2 安全知识库...
    3.2.3 多智能体协同...
```
或者更简单：把框架图放在定义节之后、知识库节之前，并在图前加一句"基于上述定义，总体框架如图所示"。

---

## 三、次要可选改动（不影响结构，可按需执行）

### 可选1：定义4与评价机制合并
当前定义4（三维质量向量）在3.2.1，评价机制（共识排序、Q函数、权重配置）在3.2.3末尾。可将定义4移至3.2.3"基于排序的质量评价"子节开头，一处读完所有评价相关内容。

### 可选2：漏洞分类叙述压缩
四类成因各有一段展开说明，篇幅较重。可将正文中的详细说明压缩为一段，把具体条目保留在表格中，使知识库节更紧凑。

### 可选3：权重说明收拢
α权重的默认设置（0.5/0.3/0.2）和敏感性分析说明目前分散在两段，可合并为一段集中处理。


## 一、当前结构梳理

```
3.1 研究动机
3.2 方法框架
    3.2.1 基本概念定义
        基本对象（定义1/2/3/3.1/3.2/3.3）
        质量评价指标（定义4）
    3.2.2 云原生权限提升安全知识库
        数据采集
        漏洞筛选
        漏洞分类（框架介绍 + 四类成因详述）
        知识库构建
    3.2.3 基于多智能体协同的策略生成与优化
        安全上下文构建
        基于漏洞分析思维链的策略生成
        基于评审反馈的策略优化
        基于排序的质量评价
3.3 方法示例（CVE-2024-33522 完整走读）
3.4 小结
```

---

## 二、主要逻辑问题

### 问题1：方法框架节过重，结构层次失衡

整个方法主体（知识库 + 生成 + 评价）都塞进 3.2 一个 section，使该节膨胀为论文篇幅最大的部分，且子节彼此独立、长度悬殊。相比之下，3.1（动机）和 3.3（示例）很简短。这会让读者感觉论文结构是"1+1个大包+1"，缺乏均衡感。

### 问题2：定义 4（质量向量）与评价机制描述分离

定义 4 放在"基本概念定义"节（约 3.2.1），而质量评价的具体机制（共识排序、分歧度量 U、总质量函数 Q、权重配置）出现在约 3.2.3 末尾的"基于排序的质量评价"子节。两处相距甚远，读者在阅读评价节时需要回头找定义。

### 问题3：漏洞分类内容偏重，与方法主线关系不够直接

"漏洞分类"子节介绍了两级成因分类框架，并对 4 个一级类分别展开说明，篇幅较长。但分类结果在正文中的使用方式（主要是"作为提示词约束"）相对有限。如果读者不了解这是数据准备的一部分，容易误以为这是一个独立研究贡献。

### 问题4："基本概念定义"与"方法框架概述"顺序微妙

当前先给出框架图，再给出定义，逻辑上略显颠倒——框架图中出现的 π、c、Kv 等符号此时尚未定义。对于初次阅读者，先定义再引出框架会更自然。

---

## 三、建议的论述顺序（思路）

以下给出两个方向，供参考：

### 方向 A：将 3.2 拆分为多个并列节（幅度较大）

```
3.1 研究动机
3.2 形式化定义与问题建模
    漏洞实例、安全上下文、策略三元组（定义1/2/3）
    质量评价指标（定义4）以及子定义3.1/3.2/3.3
3.3 方法框架概述（框架图 + 简要说明各模块职责）
3.4 安全知识库构建
    数据采集 → 筛选 → 分类 → 知识库
3.5 多智能体策略生成与评价
    安全上下文构建
    漏洞分析与策略生成（含思维链）
    评审反馈与优化
    基于排序的质量聚合
3.6 方法示例
3.7 小结
```

优点：层次清晰，读者能逐节跟随；定义在框架之前，符合形式化论文规范。  
缺点：节数增多，可能超过学校对节数的隐性要求；需要在 3.3 与后续节之间做好过渡。

---

### 方向 B：维持两层节结构，调整子节顺序（幅度较小）

```
3.1 研究动机
3.2 基本定义与方法框架
    3.2.1 形式化定义（合并所有定义，包括定义4与子定义）
    3.2.2 方法框架概述（框架图在定义之后出现）
3.3 安全知识库构建
    数据采集 → 筛选 → 分类 → 知识库
3.4 基于多智能体协同的策略生成与评价
    安全上下文构建
    漏洞分析与策略生成
    评审反馈与优化
    质量评价与排序
3.5 方法示例
3.6 小结
```

优点：改动最小，逻辑流更顺畅，定义 4 和评价机制同在一个节内。  
缺点：3.3 和 3.4 仍然较重，相比 3.1/3.2/3.5 偏长。

---

## 四、其他细节建议

1. **漏洞分类**：可考虑将四类成因的逐条说明压缩为一段紧凑的叙述，或移至附录，正文只保留分类框架表格和一段概括性描述，以使知识库节更紧凑。

2. **质量评价权重部分**：α_E > α_I > α_D 的默认设置和敏感性分析说明目前偏散，可以集中在一段内处理，避免与共识排序公式的推导混在一起。

3. **方法示例节**：目前已经写得比较完整，逻辑是"步骤1→2→3→4"，建议在小节首段先点出示例的目的（用于验证/说明方法闭环），让读者明确这是演示性内容而非实验结果。

4. **小结**：现有小结已涵盖主要模块，但顺序与正文略有错位（"多源排名聚合"说法在正文中并未作为独立术语使用）。建议统一术语后再写小结。
